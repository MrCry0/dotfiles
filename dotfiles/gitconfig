[user]
    name = Oleksandr Suvorov
    email = cryosay@gmail.com
    signingkey = 21317A636DA16768
[blame]
    # date = relative
[color]
    ui = true
    branch = always
    diff = true
    interactive = always
    grep = true
    pager = true
    decorate = always
    showbranch = always
[checkout]
    defaultRemote = upstream
[core]
    excludesfile = ~/.gitexcludes
    autocrlf = false
    compression = 0
    editor = vim
[merge]
    tool = vimdiff
    ff = only
[push]
    default = tracking
[diff]
    algorithm = histogram
    compactionHeuristic = true
[http]
    cookiefile = ~/.gitcookies
    version = HTTP/1.1
    postBuffer = 1048576000
    maxRequestBuffer = 100M
[merge]
    tool = vimdiff
[push]
    default = tracking
[pull]
    ff = only
[rerere]
    enabled = true
    autoupdate = true
[rebase]
    stat = true
[sendemail]
    confirm = auto
    smtpencryption = tls
    from = Oleksandr Suvorov <cryosay@gmail.com>
    smtpuser = cryosay@gmail.com
    chainreplyto = true
    smtpserver = smtp.gmail.com
    # smtpserver = "/usr/bin/msmtp"
    smtpserverport = 587
    smtppass =
[commit]
    gpgsign = true
[alias]
    lo = log --graph --pretty=format:'%C(auto)%h%C(auto)%d %s %C(dim white)(%aN, %ar)'
    l = log --graph --color --pretty=format:'%C(bold yellow) %C(reset) %C(red)%h%C(reset) -%C(yellow)%d%C(reset) %s %C(green)(%cr) %C(blue)<%an>%C(reset)'
    c = log -n 1 --pretty='%h (\"%s\")'
    cis = commit -S
    cu = log -n 1 --pretty="format:%h;%ai;;;;;;;%s%n"
    cul = log --pretty="format:%h;%ai;;%s"
    logs = log --show-signature
    wdiff = diff --color-words
    lg = "!f() { \
        git log --all --color --graph --pretty=format:'%C(bold yellow)<sig>%G?</sig>%C(reset) %C(red)%h%C(reset) -%C(yellow)%d%C(reset) %s %C(green)(%cr) %C(blue)<%an>%C(reset)' | \
        sed \
        -e 's#<sig>G</sig>#Good#' \
        -e 's#<sig>B</sig>#\\nBAD \\nBAD \\nBAD \\nBAD \\nBAD#' \
        -e 's#<sig>U</sig>#Unknown#' \
        -e 's#<sig>X</sig>#Expired#' \
        -e 's#<sig>Y</sig>#Expired Key#' \
        -e 's#<sig>R</sig>#Revoked#' \
        -e 's#<sig>E</sig>#Missing Key#' \
        -e 's#<sig>N</sig>#None#' | \
        less -r; \
    }; f"
